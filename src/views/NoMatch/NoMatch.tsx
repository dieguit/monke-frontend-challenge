const NoMatch = () => (
  <div className="container prose text-center md:text-left">
    <h1 className="prose-headings:h1">
      Sorry could not find what you are looking for!
    </h1>
  </div>
);

export default NoMatch;
